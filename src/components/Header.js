import React from 'react';

export default function Header(props) {
  return (
    <div className="jumbotron col-sm-12">
      <h3 style={{ textTransform: 'capitalize' }}>
        Hello,
      </h3>
    </div>
  );
}

/**
 * Hint 1 - JS expressions can be ran inside curly braces Hello, {...}
 *
 *
 *
 *
 *
 *
 *
 *
 * Hint 2 - Right above the return statement type console.log(props);
 *
 *
 *
 *
 *
 *
 *
 *
 * Hint 3 - Google the ternary operator or use the OR operator || to display visitor if input is blank
 */
